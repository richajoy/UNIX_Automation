---
   - name: Set SELINUX to disabled for DB servers
     lineinfile: 
       dest: '/etc/selinux/config'
       regexp: '^SELINUX='
       line: 'SELINUX=disabled'

   - name: Need for reboot after applying the DB requisites
     debug: msg="Fix the encountered manual errors if any and reboot the server(s)"

