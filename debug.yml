--- #Playbook for Installing Apache
- hosts: sandbox
  remote_user: rjoy
  become: yes
  become_method: sudo
  connection: ssh 
  gather_facts: no
  tasks:
 - name: Installing httpd
   yum:
      name: httpd
      state: latest
      register: result
    - debug: var=result 
